### 柯里化
将多个函数使用一个参数的函数转换为一系列使用一个参数的函数技术
```
function add(a, b) {
    return a + b
}

add(1,2)

// 柯里化
var addCurry = curry(add)
addCurry(1)(2)

```

### 实现
```
// 第一版
function curry(fn) {
    let args = [].slice.call(arguments, 1)

    return function() {
        let newArgs = args.concat([].slice.call(arguments))

        return fn.apply(this, newArgs)
    }
}

// 第二版
function sub_curry(fn, args) {
    var length = fn.length

    args = args || []

    return function() {
        var _args = args.slice(0);

        for(let i = 0; i < argumengts.length; i++) {
            let arg = arguments[i]
            _args.push(arg)
        }

        
        if (_args.length < length) {
            return sub_curry.apply(this, fn, _args)
        } else {
            return fn.apply(this, _args)
        }
    }
}
```